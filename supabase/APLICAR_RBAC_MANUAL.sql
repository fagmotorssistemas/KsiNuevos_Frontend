-- =============================================================================
-- EJECUTA ESTE ARCHIVO EN Supabase → SQL Editor si /admin/permisos está vacío
-- (tablas modules/roles no existen hasta aplicar migraciones RBAC)
-- Orden: pega y ejecuta por bloques si falla algo, o usa: npx supabase db push
-- =============================================================================

-- Si ya ejecutaste migraciones anteriores, omite las que den "already exists".
