import type { SupabaseClient } from '@supabase/supabase-js'

/** Tabla `capi_event_sync_log` — pendiente en tipos generados de Supabase. */
export function capiDb(supabase: SupabaseClient): any {
  return supabase
}
