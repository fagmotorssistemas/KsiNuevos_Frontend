import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'CAPTCHA SATJE',
}

export default function SatjeCaptchaLayout({ children }: { children: React.ReactNode }) {
  return children
}
